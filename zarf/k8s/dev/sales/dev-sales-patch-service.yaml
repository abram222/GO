apiVersion: v1
kind: Service
metadata:
  name: sales-service
  namespace: sales-system

spec:
  type: NodePort

  ports:
    - name: sales
      port: 3000
      targetPort: sales
      nodePort: 30000
    - name: sales-debug
      port: 3010
      targetPort: sales-debug
      nodePort: 30100
